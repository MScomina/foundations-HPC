[project]
name = "foundations-hpc"
version = "1.0.0"
description = "Foundations of HPC"
readme = "README.md"
requires-python = ">=3.13"
license = { text = "MIT" }

dependencies = [
    "matplotlib~=3.11.0"
]