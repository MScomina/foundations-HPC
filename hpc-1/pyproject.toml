[project]
name = "foundations-hpc-1"
version = "1.0.0"
description = "Foundations of HPC – Exercise 1"
readme = "README.md"
requires-python = ">=3.13"
license = { text = "MIT" }

dependencies = [
    "matplotlib~=3.11.0"
]

[tool.uv]
python-version = "3.13"